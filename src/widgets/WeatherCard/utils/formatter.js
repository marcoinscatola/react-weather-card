export const formatTemperature = val => Math.round(parseFloat(val)) + '°';

export const formatPercentage = val => Math.round(parseFloat(val)) + '%';

export const formatWindSpeed = val => Math.round(parseFloat(val)) + ' mph';
